# Agent 工作状态

## 当前目标

按 `my-design-dev-flow-check` 严格顺序推进个人知识问答系统设计与实现。

## 自治等级

L3 长任务自治模式。

## 已完成

- 补齐 `docs/knowledge-qa-mcp-tools.md` 中 AI Agent MCP/Agent 调用能力验收标准。
- 补齐可模拟 Agent 调用验收场景。
- 实现并验证领域规则、掌握画像、错误集、沉淀幂等、质量校验、来源冲突、API Key 鉴权、MCP schema、模拟 Agent 调用的单元测试。
- `npm run test` 通过：10 个测试文件，30 条测试用例。
- `npm run build` 通过：Next.js 生产构建成功。
- 离线安装 Playwright `chrome-headless-shell-win64.zip` 到 `chromium_headless_shell-1223`。
- `npm run test:e2e` 通过：1 条 Playwright E2E 测试。
- 创建部署运行文档、测试验证文档和用户使用说明。
- 完成文档占位符、Markdown 链接、密钥扫描和主要验证命令检查。
- Mermaid CLI 11.15.0 真实渲染 15 个 Mermaid 图为 SVG，全部通过。
- 第 20 章 Git 和交付管理检查完成。
- 用户已最终确认 `docs/knowledge-qa-design-dev-flow-check.md`。
- 新增核心知识结构 Prisma 模型：KnowledgeDomain、KnowledgeTopic、KnowledgeType、KnowledgePoint。
- 新增核心知识结构 API：domains、topics、knowledge-types、knowledge-points。
- 新增 Prisma 手工迁移 SQL：`20260607172000_add_core_knowledge_models`。
- 补充知识结构服务数据库级集成测试，使用临时 SQLite 执行迁移 SQL 并验证领域、主题、知识类型、知识点真实读写。
- 补充核心 API 路由级冒烟测试，验证 API Key、领域创建、知识类型查询、主题创建、知识点创建和列表查询。
- 新增沉淀与待确认 Prisma 模型：SourceReference、IngestionTask、ReviewItem。
- 新增外部会话沉淀 API：`POST /api/v1/ingestions/external-conversation`。
- 新增沉淀任务查询 API：`GET /api/v1/ingestions/{ingestionTaskId}`。
- 新增待确认队列 API：`GET /api/v1/review-items`、`GET /api/v1/review-items/{reviewItemId}`、confirm、reject。
- 补充外部会话沉淀路由级集成测试，验证 Agent 会话沉淀、幂等提交、确认和拒绝流转。
- 新增题库版本化 Prisma 模型：Question、QuestionVersion、AnswerVersion、ScoringRubricVersion、CoreExplanation、CoreExplanationVersion、GenerationRecord、QualityCheckRecord。
- 新增知识点 mock 生成 API：`POST /api/v1/generation/knowledge-point`。
- 新增题目查询和确认 API：`GET /api/v1/questions`、`POST /api/v1/questions/{questionId}/confirm`。
- 补充生成与题目路由级集成测试，验证核心讲解、题目、答案、评分规则、生成记录、质量校验、确认状态流。
- 新增答题评分与学习画像 Prisma 模型：AnswerAttempt、MasteryProfile、ErrorSet。
- 新增答题提交 API：`POST /api/v1/questions/{questionId}/attempts`。
- 新增用户确认/修正评分 API：`POST /api/v1/answer-attempts/{attemptId}/confirm-score`。
- 新增掌握画像和错误集查询 API：`GET /api/v1/mastery-profiles`、`GET /api/v1/error-sets`。
- 补充练习闭环路由级集成测试，验证答题、mock AI 评分、用户修正、知识点画像重算和错误集生成。
- 新增针对性练习 Prisma 模型：PracticeSession、PracticeSessionItem。
- 新增练习会话 API：`POST /api/v1/practice-sessions`、`GET /api/v1/practice-sessions/{practiceSessionId}`。
- 实现针对性选题策略：优先活跃错误集、薄弱维度，再用正式题库补齐，并返回 selection_reason。
- 扩展练习集成测试，验证基于弱项和错误集创建针对性练习会话。

## 正在进行

- 数据模型和核心 API 实现：继续扩展题库、版本化、生成质检等后续业务模型。

## 待处理

- 进入下一里程碑前确认是否提交当前实现。

## 关键决策

- 业务规则优先用纯函数和单元测试固化，后续 Prisma/API 实现复用这些规则。
- MVP 阶段 Agent 调用能力先用 schema + 本地模拟调用闭环验收，后续再接真实 MCP Server。

## 风险与阻塞

- Next.js E2E 运行时提示未来版本可能需要配置 `allowedDevOrigins`，当前不影响测试通过。
- Prisma 迁移问题已定位：SQLite `dev.db` 不存在时，schema engine 的上层命令只显示空 `Schema engine error`。
- 已直接调用 schema engine `create-database` 创建 `prisma/dev.db`，随后 `prisma db push` 成功写入 schema，并用 `migrate resolve --applied` 标记手工迁移已应用。
- `prisma/dev.db` 和 `prisma/dev.db-journal` 已由 `.gitignore` 忽略，不提交 Git。

## 验证记录

```text
2026-06-07 cmd /c npm run test：通过，10 个测试文件，30 条测试用例。
2026-06-07 cmd /c npm run build：通过。
2026-06-07 cmd /c npm run test:e2e：失败，缺少 Playwright Chromium。
2026-06-07 cmd /c npx playwright install chromium：超时。
2026-06-07 离线安装 `C:\Users\cj\Downloads\chrome-headless-shell-win64.zip`。
2026-06-07 cmd /c npm run test:e2e：通过，1 条 Playwright E2E 测试。
2026-06-07 cmd /c npm run test：通过，10 个测试文件，30 条测试用例。
2026-06-07 cmd /c npm run build：通过；并发运行 build/e2e 时曾出现一次 /_document 错误，串行重跑通过。
2026-06-07 cmd /c npm run test:e2e：通过，1 条 Playwright E2E 测试。
2026-06-07 Markdown links OK。
2026-06-07 Mermaid fences balanced。
2026-06-07 npx -y @mermaid-js/mermaid-cli --version：下载超时。
2026-06-07 mermaid-cli 11.15.0：通过复用本机 Chromium 后可运行。
2026-06-07 Mermaid render OK：15 diagrams。
2026-06-07 cmd /c npm run test：通过，10 个测试文件，30 条测试用例。
2026-06-07 cmd /c npm run build：通过。
2026-06-07 cmd /c npm run test:e2e：通过，1 条 Playwright E2E 测试。
2026-06-07 用户确认 `docs/knowledge-qa-design-dev-flow-check.md`。
2026-06-07 cmd /c npm run prisma:generate：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma validate`：通过。
2026-06-07 cmd /c npm run test：通过，10 个测试文件，30 条测试用例。
2026-06-07 cmd /c npx tsc --noEmit：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate dev --name add_core_knowledge_models`：失败，Schema engine error。
2026-06-07 直接调用 schema engine `create-database`：成功创建 `prisma/dev.db`。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma db push`：成功，数据库与 Prisma schema 同步。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate resolve --applied 20260607172000_add_core_knowledge_models`：成功。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate status`：通过。
2026-06-07 cmd /c npm run test：通过，11 个测试文件，31 条测试用例。
2026-06-07 cmd /c npx tsc --noEmit：通过。
2026-06-07 cmd /c npm run test：通过，12 个测试文件，33 条测试用例。
2026-06-07 cmd /c npx tsc --noEmit：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma validate`：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma db push`：成功，同步沉淀与待确认模型。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate resolve --applied 20260607183000_add_ingestion_review_models`：成功。
2026-06-07 cmd /c npx tsc --noEmit：通过。
2026-06-07 cmd /c npm run test：通过，13 个测试文件，36 条测试用例。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma validate`：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma db push`：成功，同步题库版本化与生成质检模型。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate resolve --applied 20260607184500_add_question_generation_models`：成功。
2026-06-07 cmd /c npm run test：通过，14 个测试文件，37 条测试用例。
2026-06-07 cmd /c npx tsc --noEmit：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate status`：通过，3 个迁移均已同步。
2026-06-07 cmd /c npm run build：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma validate`：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma db push`：成功，同步答题评分、掌握画像和错误集模型。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate resolve --applied 20260607190000_add_attempt_mastery_models`：成功。
2026-06-07 cmd /c npm run test：通过，15 个测试文件，38 条测试用例。
2026-06-07 cmd /c npx tsc --noEmit：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate status`：通过，4 个迁移均已同步。
2026-06-07 cmd /c npm run build：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma validate`：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma db push`：成功，同步练习会话模型。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate resolve --applied 20260607191500_add_practice_session_models`：成功。
2026-06-07 cmd /c npm run test：通过，15 个测试文件，39 条测试用例。
2026-06-07 cmd /c npx tsc --noEmit：通过。
2026-06-07 `$env:DATABASE_URL='file:./dev.db'; cmd /c npx prisma migrate status`：通过。
2026-06-07 cmd /c npm run build：通过。
```

## 下一步

进入下一里程碑前先由用户确认是否需要提交当前实现，或继续扩展下一组业务 API。
